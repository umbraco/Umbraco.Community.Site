@using UmbracoCommunity.Web.Models.ViewModels.Components
@model FooterViewModel

<dc-footer>
    <div class="footer-placeholder"></div>
    <footer class="sticky-footer">
        <div class="footer-inner">
            <div class="footer-inner__logo">
                <span>
                    @if (Model.Logo != null)
                    {
                        <svg-src media="Model.Logo"></svg-src>
                    }
                </span>
            </div>

            <div class="footer-inner__links">
                @if (Model.LinkColumns != null && Model.LinkColumns.Any())
                {
                    foreach (var column in Model.LinkColumns)
                    {
                        <partial name="@($"~/Views/Partials/Blocks/FooterLinkColumn.cshtml")" model="column" />
                    }
                }
            </div>
        </div>
        <div class="footer-bottom">
            <div class="footer-bottom__bottom-links">
                <ul>
                    <li>
                        <a href="bottomLink.Url" target="bottomLink.Target" title="bottomLink.Text">bottomLink.Text</a>
                    </li>
                </ul>
            </div>
            <div class="footer-bottom__social-links">
                <ul>
                    <li>
                        <a href="socialLink.Link.Url" target="socialLink.Link.Target" title="socialLink.Link.Text">
                            <img src="socialLink.Icon" width="24" height="24" alt="socialLink.Link.Text" />
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </footer>
</dc-footer>
