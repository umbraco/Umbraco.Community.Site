@using UmbracoCommunity.Web.Models.Pages
@inherits UmbracoViewPage<AccountPageViewModel>

@{
    Layout = "Layout.cshtml";
}

<div class="account-page-wrapper">
    <article class="account-page">
        <div class="account-page__band"></div>
        <div class="account-page__body">
            @if (!string.IsNullOrEmpty(Model.AvatarUrl))
            {
                <img class="account-page__avatar"
                     src="@Model.AvatarUrl"
                     alt=""
                     width="100"
                     height="100" />
            }

            <h1 class="account-page__name">@Model.DisplayName</h1>

            @if (!string.IsNullOrEmpty(Model.GitHubHandle))
            {
                <p class="account-page__handle">
                    <a href="https://github.com/@Model.GitHubHandle" target="_blank" rel="noopener noreferrer">
                        <partial name="~/Views/Partials/Icons/GitHub.cshtml" />
                        Signed in as @@@Model.GitHubHandle
                    </a>
                </p>
            }

            @if (!string.IsNullOrEmpty(Model.Email))
            {
                <dl class="account-page__details">
                    <dt>Email</dt>
                    <dd>@Model.Email</dd>
                </dl>
            }

            @if (Model.OnboardingCompleted && !string.IsNullOrEmpty(Model.ProfileUrl))
            {
                <div class="account-page__profile-cta">
                    <h2>Your public profile</h2>
                    <p>This is where the rest of the community finds you.</p>
                    <dc-profile-link url="@Model.ProfileUrl"></dc-profile-link>
                    <a class="btn is-blue account-page__view-profile" href="@Model.ProfileUrl">View your public profile</a>
                </div>
            }
            else if (!string.IsNullOrEmpty(Model.OnboardingUrl))
            {
                <p class="account-page__profile-link">
                    <a href="@Model.OnboardingUrl">Finish setting up your profile</a>
                </p>
            }

            <div class="account-page__feeds">
                <h2>Your feeds</h2>
                <dc-feed-manager></dc-feed-manager>
            </div>

            <form method="post" action="/logout" class="account-page__logout">
                @Html.AntiForgeryToken()
                <button type="submit" class="btn is-transparent-blue">Sign out</button>
            </form>
        </div>
    </article>
</div>
